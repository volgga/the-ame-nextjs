# Настройка ENV переменных в Vercel

## Пошаговая инструкция

### 1. Войдите в Vercel Dashboard

1. Откройте [https://vercel.com](https://vercel.com)
2. Войдите в свой аккаунт
3. Найдите проект `the-ame-nextjs` (или как он называется у вас)

### 2. Откройте настройки проекта

1. Кликните на название проекта
2. Перейдите в раздел **Settings** (в верхнем меню)
3. В левом меню выберите **Environment Variables**

### 3. Добавьте переменные

Нажмите **Add New** и добавьте каждую переменную по очереди:

#### Обязательные для Telegram уведомлений:

**TELEGRAM_BOT_TOKEN**
- **Value**: `8210290619:AAEXbzbTbkcR5pH-gsondkTRa165ie9ZBYs` (из вашего .env.local)
- **Environment**: Выберите все окружения (Production, Preview, Development) или только Production

**TELEGRAM_ORDERS_CHAT_ID**
- **Value**: `-1002343550030` (из вашего .env.local)
- **Environment**: Все окружения или только Production

**TELEGRAM_ORDERS_THREAD_ID** (опционально, но рекомендуется)
- **Value**: `1947` (из вашего .env.local)
- **Environment**: Все окружения или только Production

#### Fallback переменные (если TELEGRAM_ORDERS_CHAT_ID не задан):

**TELEGRAM_CHAT_ID** (fallback)
- **Value**: `-1002343550030` (из вашего .env.local)
- **Environment**: Все окружения или только Production

**TELEGRAM_THREAD_ID** (fallback)
- **Value**: `3766` (из вашего .env.local)
- **Environment**: Все окружения или только Production

#### Для Tinkoff/T-Bank:

**TINKOFF_TERMINAL_KEY**
- **Value**: `1754488339859` (из вашего .env.local)
- **Environment**: Все окружения или только Production

**TINKOFF_PASSWORD**
- **Value**: `Njtk41vKKN3yi58i` (из вашего .env.local)
- **Environment**: Все окружения или только Production

**TINKOFF_NOTIFICATION_URL**
- **Value**: `https://theame.ru/api/tinkoff-callback`
- **Environment**: Production

### 4. Сохраните изменения

После добавления всех переменных:
1. Нажмите **Save** для каждой переменной
2. Все переменные должны появиться в списке

### 5. Перезапустите деплой (важно!)

После добавления ENV переменных нужно перезапустить деплой:

**Вариант 1: Через Dashboard**
1. Перейдите в раздел **Deployments**
2. Найдите последний деплой
3. Нажмите на три точки (⋮) справа
4. Выберите **Redeploy**
5. Подтвердите перезапуск

**Вариант 2: Через Git push**
Просто сделайте любой коммит и push:
```bash
git commit --allow-empty -m "Trigger redeploy for ENV variables"
git push
```

### 6. Проверьте что переменные применились

После перезапуска деплоя:

1. Откройте диагностический endpoint:
   ```
   https://theame.ru/api/payments/tinkoff/notify/check
   ```

2. Или с orderId:
   ```
   https://theame.ru/api/payments/tinkoff/notify/check?orderId=YOUR_ORDER_ID
   ```

3. Проверьте поле `envAllSet` — должно быть `true`

4. Проверьте поле `env` — все значения должны быть `true`

## Важные замечания

### ⚠️ Безопасность

- **НЕ коммитьте** `.env.local` в Git (он уже в `.gitignore`)
- **НЕ публикуйте** токены и пароли в открытом доступе
- Используйте разные токены для Production и Development если возможно

### 🔄 Обновление переменных

Если нужно обновить значение переменной:
1. Найдите переменную в списке
2. Нажмите на неё
3. Измените значение
4. Сохраните
5. **Обязательно перезапустите деплой!**

### 📋 Полный список переменных для копирования

Если хотите добавить все сразу, вот полный список из вашего `.env.local`:

```
TELEGRAM_BOT_TOKEN=8210290619:AAEXbzbTbkcR5pH-gsondkTRa165ie9ZBYs
TELEGRAM_CHAT_ID=-1002343550030
TELEGRAM_THREAD_ID=3766
TELEGRAM_ORDERS_CHAT_ID=-1002343550030
TELEGRAM_ORDERS_THREAD_ID=1947
TINKOFF_TERMINAL_KEY=1754488339859
TINKOFF_PASSWORD=Njtk41vKKN3yi58i
TINKOFF_NOTIFICATION_URL=https://theame.ru/api/tinkoff-callback
```

## Альтернативный способ: через Vercel CLI

Если у вас установлен Vercel CLI:

```bash
# Установите Vercel CLI если еще не установлен
npm i -g vercel

# Войдите в аккаунт
vercel login

# Добавьте переменные
vercel env add TELEGRAM_BOT_TOKEN production
# Введите значение когда попросит

vercel env add TELEGRAM_ORDERS_CHAT_ID production
# И так далее для каждой переменной

# После добавления всех переменных - перезапустите деплой
vercel --prod
```

## Проверка после настройки

После добавления переменных и перезапуска деплоя:

1. **Сделайте тестовый заказ** и оплату
2. **Проверьте логи** в Vercel Dashboard → Logs
3. **Ищите логи** с префиксом `[tbank-notify]`
4. **Проверьте Telegram** — должно прийти уведомление

Если что-то не работает — откройте диагностический endpoint и пришлите результат.
