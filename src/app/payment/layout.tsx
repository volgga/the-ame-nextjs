/**
 * Layout для сегмента /payment (success, fail).
 * Гарантирует корректное разрешение маршрутов Next.js.
 */
export default function PaymentLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
